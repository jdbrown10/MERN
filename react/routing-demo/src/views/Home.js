//a view is just a component that gets sent to a route

import React from "react";

const Home = (props) => {
    return (
        <div>
            <h1>Home page here</h1>
        </div>
    )
}

export default Home;